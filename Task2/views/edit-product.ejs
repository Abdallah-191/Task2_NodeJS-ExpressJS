<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Product</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="container">
        <h1>Edit Product</h1>

        <form action="/products/edit/<%= product._id %>" method="post">
            <label for="name">Product Name</label>
            <input type="text" id="name" name="name" value="<%= product.name %>" required />

            <label for="code">Product Code</label>
            <input type="text" id="code" name="code" value="<%= product.code %>" required />

            <label for="type">Product Type</label>
            <input type="text" id="type" name="type" value="<%= product.type %>" required />

            <label for="company">Company</label>
            <input type="text" id="company" name="company" value="<%= product.company || '' %>" />

            <label for="expiryDate">Expiry Date</label>
            <input type="date" id="expiryDate" name="expiryDate" value="<%= product.expiryDate ? product.expiryDate.toISOString().split('T')[0] : '' %>" />

            <button type="submit">Save Changes</button>
        </form>
        <a href="/products" class="link" style="display: block; text-align: center; margin-top: 20px;">Cancel and Return to List</a>
    </div>
</body>
</html>
